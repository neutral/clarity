# Step: cross-link-docs

Purpose: Connect related docs to reduce duplication and drift.

Inputs:
- Target set (paths/headings/labels)

Steps:
1) Identify related docs or sections.
2) Add references where context overlaps.
3) Replace duplicated content with a single canonical source.

Outputs:
- Cross-links between related docs and reduced duplication.
