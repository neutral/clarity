# Step: apply-labels

Purpose: Apply clarity labels to statements.

Inputs:
- Target set (paths/headings/labels)
- `../../LABELS.md`

Steps:
1) Identify statement roles and natural groupings.
2) Prefer section defaults or a label map per `../../LABELS.md`.
3) Use inline labels only for mixed sections or short exceptions.
4) Add missing Definitions for overloaded terms.

Outputs:
- Section defaults or a label map, labeled statements, and a clearer glossary.
